Drop original, unprocessed photos here, then run:

```bash
python3 tools/make_portrait.py raw-photos/ --out-dir assets/images/team/
```

This folder is excluded from the build — nothing in it is published.
