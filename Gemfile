source "https://rubygems.org"

# Local development. GitHub Pages builds with its own gem environment
# when you push, so these versions only affect `jekyll serve` locally.
gem "jekyll", "~> 4.3"
gem "webrick", "~> 1.8"

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.17"
  gem "jekyll-seo-tag", "~> 2.8"
end
